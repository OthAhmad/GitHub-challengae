This challenge was created using react and express, to run the code you have to use:

// in the challenge folder run the following command:

npm start

// in anthor console navigate to server folder and run the command:

node server.js
